# Claude Code 配置文件（Windows版）
# 复制到 C:\Users\yourname\.claude\settings.json

{
  "models": {
    "default": "glm-4.5-air",
    "providers": {
      "zai": {
        "apiKey": "YOUR_API_KEY_HERE",  # 替换为您的Z.ai API Key
        "baseURL": "https://open.bigmodel.cn/api/paas/v4/chat/completions"
      }
    }
  }
}
